import { createContentPageRoute } from '@/app/_shared/create-content-page-route'

const { Page, generateMetadata } = createContentPageRoute('chi-siamo')
export { generateMetadata }
export default Page
